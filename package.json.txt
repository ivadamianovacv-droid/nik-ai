<!DOCTYPE html>
<html lang="bg">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Nik AI Chatbot</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f3f3f3; margin: 0; padding: 20px; }
    #chatbox { width: 100%; max-width: 600px; margin: auto; background: #fff; padding: 20px; border-radius: 10px; height: 70vh; overflow-y: auto; }
    .msg { margin: 10px 0; padding: 10px; border-radius: 8px; }
    .user { background: #d1e7ff; text-align: right; }
    .bot { background: #e9e9e9; }
    #inputArea { display: flex; max-width: 600px; margin: 10px auto; }
    input { flex: 1; padding: 10px; font-size: 16px; border-radius: 8px; border: 1px solid #aaa; }
    button { padding: 10px 20px; margin-left: 10px; border: none; border-radius: 8px; background: #007bff; color: white; cursor: pointer; }
    button:hover { background: #005fcc; }
  </style>
</head>
<body>
  <div id="chatbox"></div>

  <div id="inputArea">
    <input id="msg" placeholder="Попитай нещо за комунизма..." />
    <button onclick="sendMsg()">Испрати</button>
  </div>

  <script>
    async function sendMsg() {
      const input = document.getElementById('msg');
      const text = input.value.trim();
      if (!text) return;

      addMsg(text, 'user');
      input.value = '';

      const response = await fetch('https://YOUR_BACKEND_URL/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ message: text })
      });

      const data = await response.json();
      addMsg(data.reply, 'bot');
    }

    function addMsg(text, who) {
      const box = document.getElementById('chatbox');
      const div = document.createElement('div');
      div.className = 'msg ' + who;
      div.innerText = text;
      box.appendChild(div);
      box.scrollTop = box.scrollHeight;
    }
  </script>
</body>
</html>

// package.json
{
  "name": "nik-ai-backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "openai": "^4.0.0",
    "dotenv": "^16.4.0"
  }
}
